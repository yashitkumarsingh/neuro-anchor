{
  "name": "@neuro-anchor/core",
  "version": "0.1.0",
  "description": "Shared core logic, models, and heuristics for neuro-anchor extension and CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build": "npm run compile"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "@types/node": "^18.11.18"
  }
}
